# 이세계로 소환되어버린 나무꾼 v0.9 배포 인수인계

## v0.9 최우선 변경 사항

아래 항목이 v0.8 이하 설명과 충돌하면 v0.9 내용을 우선합니다.

- 하단 HUD는 좌측 상태·장비 40%, 우측 로그 60%의 좌우 그리드입니다.
- 장착 장비는 최대 72px 크기의 2열 슬롯으로 배치하고 다섯 번째 갑옷 슬롯은 가운데에 둡니다.
- 로그는 본문 13px·아이콘 32px·항목 최소 높이 46px을 유지하면서 더 넓은 가로 공간을 사용합니다.
- 로그, 오버레이, 가로 탭, 마이페이지 장비함은 스크롤 기능을 유지하되 브라우저/WebView 스크롤바를 표시하지 않습니다.
- `main` 푸시와 수동 실행 시 Windows 설치 파일과 Android 범용 APK를 만드는 GitHub Actions를 제공합니다.
- 버전과 저장 키는 `0.9.0`, `isekai_lumberjack_save_v09`입니다.
- 신규 게임은 배포 기본값인 레벨 1·재화 0을 유지합니다.

## v0.8 최우선 변경 사항

아래 항목이 v0.7 이하 설명과 충돌하면 v0.8 내용을 우선합니다.

- 메인 메뉴는 기본적으로 접혀 있습니다. 우상단 48px 토글을 누르면 52px 아이콘 8개가 4×2로 펼쳐지며 텍스트 라벨은 접근성 정보로만 유지합니다.
- 대상 이름·체력 바·숫자는 대상 이미지에서 분리한 상단 HUD입니다. 메뉴가 펼쳐질 때 HUD가 아래로 이동해 겹치지 않습니다.
- 상태 영역 아래의 획득 로그는 HUD 전체 너비를 사용하고, 본문 13px·아이콘 32px·항목 최소 높이 46px를 사용합니다.
- `lastSeen` 절대 시각과 남은 밀리초를 보존해 브라우저/모바일 WebView 타이머가 정지한 기간도 복귀 시 정산합니다.
- `visibilitychange`, `freeze/resume`, `pagehide/pageshow`, `focus`에서 같은 정산 절차를 호출합니다.
- 숲·광산·던전 모두 동일한 강화의 돌 확률을 적용합니다. 하급 지역은 하급 5%, 중급 지역은 하급 3%·중급 2%, 상급 지역은 하급 5%·중급 3%·상급 1%입니다.
- BGM과 효과음의 신규 기본값은 모두 50%입니다. BGM 출력에는 1.4배 체감 보정을 적용하되 최종 볼륨은 100%로 제한합니다.
- 설정의 수동 저장 버튼을 제거했습니다. 게임 진행과 음량 설정은 자동 저장됩니다.
- 버전과 저장 키는 `0.8.0`, `isekai_lumberjack_save_v08`입니다.
- QA용 레벨 100·재화 9,999 시작값은 계속 비활성화되어 신규 게임은 레벨 1·재화 0으로 시작합니다.

## v0.7 구현 이력 — v0.8 이상 규칙과 충돌하면 적용하지 않음

아래 항목이 이전 버전 설명과 충돌하면 아래 내용을 우선합니다.

- QA 시작값을 해제했습니다. 신규 게임은 레벨 1, 체력 500, 모든 재화·강화돌·낚시 재료 0으로 시작합니다.
- 낚싯대별 어종 확률은 v0.6 값을 변경하지 않았습니다.
- 제작 성공 즉시 1초 제작 잠금과 `제작 완료!` 안내를 적용해 빠른 중복 클릭을 차단합니다.
- 허름한 장비는 장착 여부와 관계없이 버릴 수 없습니다.
- 강화의 돌은 던전에서만 드롭합니다. 하급 던전은 하급 5%, 중급 던전은 하급 3%·중급 2%, 상급 던전은 하급 5%·중급 3%·상급 1%입니다.
- 집에서 화면 또는 오토 버튼을 누르면 휴식 자세가 고정되며 다시 누르면 해제됩니다. 휴식 중에만 10초 회복이 진행되고, 앱 종료 시간도 정산합니다.
- 휴식 화면에는 총 경과 시간, 다음 회복까지 남은 시간, 10초당 회복량을 표시합니다.
- 엔딩 크레딧은 첫 엔딩 이미지가 나온 지 3초 뒤부터 이미지 위로 함께 올라갑니다.
- 엔딩 크레딧과 5초 대기가 끝나면 일반 `save.json`을 자동 삭제합니다. 이후에는 해당 엔딩을 다시 볼 수 없고 새 게임만 시작합니다.
- 엔딩 목격 기록은 별도 `meta.json`에 보존합니다. 이후 모든 새 게임에는 장착·강화·버리기가 불가능하고 보관 한도에도 포함되지 않는 `이스터에그` 징표가 지급됩니다.
- 신규 이미지: `assets/items/easter_egg.png`.

## v0.6 구현 이력 — v0.7 이상 규칙과 충돌하면 적용하지 않음

아래 항목이 이 문서의 이전 버전 설명과 충돌하면 아래 내용을 우선합니다.

- QA 세이브는 레벨 100, 현재 체력 3,325, 모든 재화·강화돌·낚시 재료 9,999로 시작합니다.
- 모든 재화·강화돌·낚시 재료의 소지 상한은 9,999이며, 초과 획득 로그는 정상 표시하되 실제 수량은 더 늘지 않습니다.
- 생선 수프 회복량은 +50입니다.
- 체력은 숫자가 내부에 표시되는 체력 바로 렌더링합니다.
- 귀환문은 레벨 100 또는 신의 장비 5종 보유 시 영구 해금됩니다.
- 귀환 확인 후 2초 암전과 `나를 베고 가라.` 도전을 거쳐 최종 전투를 시작합니다.
- 최종 세계수는 HP 4,444,444, 방어력 5,000이며 매 타격마다 500~1,500 범위 정규분포 반사 피해를 줍니다.
- 세계수 전투에서 체력이 0이 되어도 장소에서 추방되지 않으며 요리로 회복할 수 있습니다. 장소를 떠나면 세계수 HP는 초기화됩니다.
- 신규 세이브는 현대 벌목장 5초, 덤프트럭 접근 5초, 검은 화면 3초, 이세계의 허름한 집 기상 5초 순서의 오프닝을 재생합니다.
- 엔딩은 터널 귀환 이미지 10초, 기존 노을 이미지 10초, 38초 상승 크레딧을 재생한 뒤 5초 후 종료·인트로 버튼을 표시합니다.
- 낚싯대도 +10 강화할 수 있으며 강화 배율만큼 낚시 정규분포 시간이 단축됩니다. 기본 평균 15초, +4 신의 낚싯대 평균 약 8.3초입니다.
- 장비 보관 한도는 20개입니다. 마이페이지 장비 목록은 내부 스크롤이며, 미장착 장비는 확인 후 버릴 수 있습니다.
- 신규 이미지: `worldtree_cave.png`, `worldtree.png`, `story_intro1~3.png`, `ending_return.png`.

## 이번 버전의 기준

`GAME_DESIGN_MASTER_v0.9.md` 마지막의 v0.9 확정 변경 사항이 이전 장과 충돌할 경우 우선합니다.

- 장비 명칭을 `검`으로 통일
- HUD는 현재 작업에 사용하는 장비를 포함한 `총공격력`만 표시
- 기본 체력 500, 갑옷 장착 보너스를 최대 체력에 합산
- 체력 0에서 자동 귀가하지 않으며 모든 행동 탭에 휴식 안내 표시
- 낚시 1회당 체력 1 소모, 경험치 없음
- 오토 낚시는 획득 1초 뒤 다음 낚시 시작
- 종료·백그라운드 정산은 오토가 켜진 장소 작업만 수행하고 체력 0에서 오토 해제
- 집의 종료·백그라운드 회복은 휴식이 켜진 상태에서만 10초 틱으로 정산
- 종료 정산 결과는 팝업 없이 메인 로그에 시간순 기록
- 항상 인트로부터 시작하며 최근 접속·레벨·체력·초기화·종료 표시
- 앱 버전이 바뀌면 기존 세이브 초기화
- 제작소·부동산·요리·강화·마이페이지를 동일한 2차 레이어로 구성
- 던전 대상은 하급 고블린, 중급 마물, 상급 드래곤

## 구현된 플레이 루프

1. 지도에서 집·숲·광산·연못·던전으로 이동합니다.
2. 숲·광산·던전은 탭 또는 1초 오토 행동으로 체력을 소비합니다.
3. 대상 체력과 보상은 저장된 난수 시드를 사용하므로 종료 정산에서도 정상 계산됩니다.
4. 획득한 목재·광석·금화로 제작소에서 도구·검·갑옷을 제작합니다.
5. 강화의 돌로 장비를 강화하고 마이페이지에서 장착합니다.
6. 낚시 재료는 요리로 바꿔 즉시 체력을 회복할 수 있습니다.
7. 부동산에서 집을 구입하고 휴식처를 지정해 장기 회복 속도를 높입니다.
8. 레벨 100을 달성하거나 신의 장비 5종을 모두 보유하면 지도에 원래세계 귀환 버튼과 엔딩이 영구 해금됩니다.

## 저장과 경과 시간 정산

- Tauri: 운영체제 앱 데이터 경로의 일반 진행 `save.json`과 엔딩 목격 기록 `meta.json`
- 브라우저 미리보기: 일반 진행과 엔딩 목격 기록을 분리한 `localStorage`
- 저장 내용: 플레이어 상태, 장비, 재화, 로그, 현재 대상, 낚시 상태, 오토 상태, 난수 시드, 마지막 저장 시각
- 앱 종료 중 별도 프로세스나 서버는 동작하지 않습니다. 다음 실행 시 마지막 저장 시각부터 경과분을 빠르게 시뮬레이션합니다.
- 필드 오토는 초당 1행동이며 현재 체력만큼만 정산합니다.
- 낚시는 저장된 입질 종료 시각과 1초 재투척 간격을 반복 계산합니다.
- 체력이 0이 되면 저장된 오토 상태도 OFF가 됩니다.

## 이미지와 오디오

새로 보강한 런타임 에셋:

- 던전 배경 3장
- 하급 고블린·중급 마물·상급 드래곤 3종
- 나무·광맥 작업 대상 6종 정규화
- 갑옷 5종, 강화의 돌 3종, 로고와 메뉴 아이콘
- 기존 시트에서 분리한 장비 20종, 재화 15종, 요리 5종, 행동 프레임 18종
- 사용자 제공 BGM 8곡: 타이틀·집·숲·광산·연못·던전·지도·엔딩
- 48 kHz 스테레오 Vorbis 효과음 34종

효과음은 도끼·곡괭이·검 스윙/타격, 대상 파괴, 낚시 5단계, 전리품, 제작·구입·장착, 요리·회복, 강화 시작/성공/실패/파괴, 레벨업, 오토, UI, 체력 부족, 월드 해금으로 구성했습니다. 재생용 OGG는 `assets/audio/sfx/`, 재생성 스크립트는 `tools/generate_sfx.py`에 있습니다.

## 검증 결과

2026-09-02 기준:

- `node --check game.js`: 통과
- `node tools/test_game.js`: 통과
- `node tools/prepare_dist.js`: 통과
- `python tools/validate_project.py`: 602개 검사 통과
- `package.json`, `tauri.conf.json`: JSON 파싱 통과
- GitHub Windows 워크플로: YAML 파싱 통과
- 이미지 22개 배경과 모든 정규화 에셋: 파일·해상도·디코딩 검사 통과
- BGM 8곡과 효과음 34개: 오디오 스트림·길이 검사 통과

스모크 테스트가 다루는 항목:

- 레벨 1·체력 500·재화 0 배포 시작값과 신의 갑옷 +4 최대 체력 계산
- 기존 낚싯대별 어종 확률 불변 검사
- 신의 장비 5종 영구 귀환문 해금
- 낚싯대 강화 배율과 평균 낚시 시간 단축
- 필드 오토의 체력 소모와 체력 0 오토 해제
- 오토 OFF 상태의 종료 중 미진행
- 낚시 투척 무소모, 획득 시 체력 1 소모·경험치 0·보상 획득
- 낚시 오토 종료 정산과 체력 0 오토 해제
- 휴식 ON/OFF와 집 10초 회복 틱 종료 정산
- 숲·광산·던전 공통 등급별 강화돌 확률 경계값
- 360×640, 390×700, 430×932, 560×900 모바일 레이아웃 정적 검사
- 접이식 메뉴·분리 대상 체력 HUD·좌우 HUD·대형 장비 아이콘·숨김 스크롤바·수동 저장 버튼 제거 검사
- 제작 완료 잠금과 허름한 장비 버리기 차단
- 엔딩 일반 세이브 삭제와 영구 이스터에그 메타 기록
- 재화 9,999 상한과 장비 보관함 20칸 제작 차단
- 세계수 반사 피해와 격파 완료 상태
- 제작소·부동산·요리·낚싯대 포함 강화·마이페이지 렌더링

## 빌드와 실기기 확인

로컬 인수인계 환경에 Rust와 Android SDK/NDK가 없어도 GitHub Actions의 Windows·Android 실행 환경에서 설치 파일을 생성하도록 구성했습니다.

- Windows: `BUILD_WINDOWS.bat`
- Android: `BUILD_ANDROID.bat`
- Windows 자동 빌드: `.github/workflows/windows-build.yml`
- Android 범용 APK 자동 빌드: `.github/workflows/android-build.yml`

첫 실기기 빌드에서는 다음을 확인합니다.

1. Windows WebView2와 Android WebView의 오디오 자동 재생 정책
2. Android 시스템 뒤로가기의 2단계 종료 안내
3. 강제 종료 후 `save.json` 경과 시간 정산
4. 백그라운드 복귀 시 BGM 재개와 오토 정산
5. 390×700부터 태블릿 크기까지 UI 잘림 여부
6. APK/AAB 서명과 스토어용 앱 아이콘·스플래시 최종 규격
