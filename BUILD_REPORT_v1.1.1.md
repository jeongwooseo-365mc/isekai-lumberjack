# 이세계로 소환되어버린 나무꾼 v1.1.1 빌드 보고서

## 수정 원인

- v1.1 Android 빌드는 `tauri android init`보다 먼저 아이콘 명령을 실행해, 이후 초기화 과정에서 Tauri 기본 아이콘이 다시 생성되었습니다.
- v1.1 APK 내부 리소스를 추출해 확인한 결과 도끼 대신 Tauri 기본 로고가 패키징되어 있었습니다.
- QA APK는 실행마다 새 테스트 키로 서명되므로 이전 APK 위에 업데이트 설치할 수 없습니다. v1.1.1 설치 전 기존 테스트 앱을 한 번 제거해야 합니다.

## v1.1.1 검증 항목

- Android 초기화 후 모바일 아이콘 생성
- 5개 해상도의 일반·전경 도끼 아이콘 10개 해시 검증
- 최종 APK 내부 도끼 아이콘 및 ARM64 라이브러리 검증
- x86_64 검증 APK를 Android API 29 에뮬레이터에 설치
- 앱 시작 후 프로세스 생존, MainActivity 활성 상태, crash 로그 부재 확인
- Android API 29 WebView용 `100vh` 높이 호환 처리 및 빈 화면 캡처 자동 거부
- 최초 전체화면 시스템 안내를 제거한 실제 게임 인트로 화면 캡처 확인
- Windows NSIS 재빌드 및 콘솔 창 미표시 설정 유지
- v1.1 세이브 형식 호환

실제 빌드 실행 번호, 파일 크기와 SHA-256은 재배포 완료 후 기록합니다.
