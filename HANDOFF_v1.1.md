# 이세계로 소환되어버린 나무꾼 v1.1 인수인계

## 기준 버전

- 앱 버전: `1.1.4`
- 일반 세이브 키: `isekai_lumberjack_save_v11`
- 신규 게임: 레벨 1, 체력 500, 모든 재화·강화돌·낚시 재료·음식 0개
- QA용 레벨 100·재화 9,999 시작값: 비활성화
- 저장 정책: 버전 불일치 시 일반 세이브 초기화, 엔딩 메타는 별도 보존

## v1.1 확정 변경

### 휴식

- 모든 집의 기존 회복량은 유지하고 회복 주기만 10초에서 1초로 변경했습니다.
- 휴식 상태 문구는 총 경과 시간과 `1초당 +회복량`만 표시합니다. 다음 회복 카운트다운은 없습니다.
- 화면 탭 또는 오토 버튼으로 휴식 자세를 켜고 끕니다.
- 휴식이 켜져 있으면 앱 종료·백그라운드 경과 초를 절대 시각 기준으로 정산합니다.

| 집 | 1초당 회복 |
|---|---:|
| 초라한 오두막 | +1 |
| 괜찮은 목조주택 | +2 |
| 넓은 전원주택 | +5 |
| 고급 저택 | +20 |
| 귀족풍 대저택 | +100 |

### 요리와 음식 인벤토리

| 음식 | 회복량 | 필요 재료 |
|---|---:|---|
| 생선 수프 | +75 | 해초 100, 민어 20 |
| 해산물 스튜 | +225 | 해초 100, 조개 100, 민어 50 |
| 구운 생선 | +100 | 숭어 10 |
| 연어 스테이크 | +200 | 연어 10 |
| 고급 랍스터 정식 | +400 | 랍스터 10 |

- `만들어 먹기`를 `요리하기`로 변경했습니다.
- 요리 성공 시 체력은 변하지 않고 해당 음식이 1개 늘며 `음식명 1개를 만들었습니다.` 안내가 표시됩니다.
- 모든 재화·강화돌·낚시 재료·음식은 종류별 99,999개까지 보유합니다.
- 보관함은 장비와 보유 음식 종류를 합쳐 40칸이며 `보유 장비 현재/40`으로 표시합니다.
- 마이페이지 장비 내부 스크롤 목록에 음식이 `음식명 x수량`으로 함께 표시됩니다.
- 음식 상세 버튼은 미장착 시 `장착하기/즉시먹기`, 장착 시 `장착 중/장착 해제`입니다.
- 새로 제작한 장비·음식과 강화 성공으로 수치가 바뀐 장비는 확인 전까지 연한 테두리로 표시합니다.
- 장착 음식은 체력이 0이 되는 순간 1개를 자동 사용합니다. 회복되면 오토를 유지합니다.
- 마지막 1개를 먹으면 장착 음식 슬롯이 비고 마이페이지 목록에서도 해당 음식이 사라집니다.
- 메인 HUD는 도끼·곡괭이·낚싯대·검·갑옷·음식의 2열 6칸입니다.

### 모바일·Windows 셸

- Android 표시 이름은 `이세계나무꾼`입니다.
- Windows는 `src-tauri/icons/icon.png` 원본을 사용하고 Android는 둥근 마스크 안전 여백을 더한 `src-tauri/icons/icon-mobile.png`를 사용합니다.
- Android v1.1.3 식별자는 `com.isekailumberjack.stable`이며 충돌하던 이전 테스트 앱과 별도 설치됩니다. Windows 식별자는 기존 값을 유지합니다.
- Android 15에서는 `window.insetsController`를 `onCreate`에서 즉시 읽으면 장식 뷰 생성 전 NPE가 발생합니다. 몰입 화면은 `window.decorView.post` 이후 `decorView.windowInsetsController`로만 적용합니다.
- Android 자동 QA는 API 35에서 게임 시작을 탭하고 오프닝 3장과 메인 게임 진입까지 검증합니다.
- `tools/patch_android.py`가 생성된 `MainActivity.kt`에 시스템 상태·내비게이션 바 숨김을 적용합니다.
- Android 11 이상은 스와이프로 시스템 바를 일시 표시하는 transient-bars 동작을 사용하고 구버전은 immersive-sticky 플래그를 사용합니다.
- Android 초기화 뒤에는 항상 패치 스크립트를 실행해야 합니다. 로컬 배치 파일과 GitHub Actions에 이 단계를 포함했습니다.
- Windows 릴리스는 `windows_subsystem = "windows"`로 빌드되어 콘솔 창을 열지 않습니다.

### 입력과 지도

- 메인 장면 입력을 `pointerdown`에서 모바일 WebView의 합성 `click` 경로로 통일했습니다.
- 연못에서 오토를 켜지 않아도 화면을 한 번 탭하면 `fishState`가 생성되고 투척 애니메이션·입질 타이머가 시작됩니다.
- 던전 지도 버튼은 `left: 86%; top: 88%`로 내려 우하단 암벽산에 맞추고 모서리 여백을 남겼습니다.

### v1.1 드롭·제작 수치

- 상급 숲·광산·던전은 하급 일반 재화와 하급 강화돌을 드롭하지 않습니다.
- 상급 일반 재화는 기존 상급:중급 비율 20:35를 정규화한 약 36.36%:63.64%입니다.
- 강화돌은 하급 지역 `하급 10%`, 중급 지역 `하급 2%/중급 8%`, 상급 지역 `중급 3%/상급 7%`입니다.
- 신의 장비 5종 제작 재료는 v1.0 대비 모두 2배입니다.
- 경험치가 1,000 이상이면 버림한 정수 `k` 표기로 표시하며 백만 이상도 `k`를 유지합니다.
- 낚싯대 어종 표는 지정 가중치를 사용하고, 합계가 105와 101인 영웅·신의 행은 합계 기준으로 정규화합니다.

## 유지되는 핵심 규칙

- 접이식 4×2 아이콘 메뉴와 메뉴 전개 시 대상 체력 HUD 회피
- 로그·오버레이·장비함의 터치 스크롤 유지 및 시각적 스크롤바 숨김
- 숲·광산·던전 공통 강화돌 드롭 확률
- 낚시 투척 시 체력 무소모, 어획 완료 시 체력 1 소모, 경험치 없음
- 도끼·곡괭이·낚싯대·검·갑옷 +10 강화 및 실패 파괴 규칙
- 장비·음식 종류 통합 보관 40칸, 허름한 장비와 이스터에그 버리기 금지
- 레벨 100 또는 신의 장비 5종 보유 시 귀환문 영구 해금
- 최종 세계수 HP 4,444,444, 방어력 5,000, 타격마다 500~1,500 정규분포 반사 피해
- 엔딩 완료 후 일반 세이브 삭제, 이후 새 게임에 이스터에그 지급
- BGM·효과음 초기 설정 50%, BGM 체감 출력 1.4배 보정

## 방치 정산

- 저장된 `lastSeen` 절대 시각에서 완전히 지난 초만 정산하고 밀리초 나머지를 보존합니다.
- 숲·광산·던전·세계수는 오토 ON일 때 초당 1행동을 시뮬레이션합니다.
- 연못은 저장된 입질 종료 시각과 재투척 간격을 반복 계산합니다.
- 장착 음식이 있으면 방치 행동 중 체력 0에서도 자동 사용하고 남은 시간의 작업을 계속합니다.
- 음식이 없고 체력이 0이면 오토를 끄고 이후 작업을 중단합니다.
- 집은 휴식 ON일 때 경과 1초마다 선택한 집의 회복량을 더합니다.
- `visibilitychange`, `freeze/resume`, `pagehide/pageshow`, `focus`가 같은 정산 함수로 진입합니다.

## 배포

- Windows·Android 통합 자동 빌드: `.github/workflows/release-build.yml`
- Windows 로컬 빌드: `BUILD_WINDOWS.bat`
- Android 로컬 빌드: `BUILD_ANDROID.bat`
- GitHub `main` 푸시 시 하나의 Actions 실행에서 Windows NSIS와 ARM64 영구 서명 APK를 병렬 생성합니다.
- Android 앱 ID와 인증서 지문은 `SIGNING.md`에 고정되어 있으며, 워크플로가 매 빌드에서 지문을 검증합니다.

## 검증 범위

`tools/test_game.js`는 다음을 회귀 검사합니다.

- v1.1 배포 시작값과 레시피·낚시·드롭·제작 수치
- 1초 휴식 및 백그라운드 절대 시각 정산
- 수동 탭 낚시 시작과 어획 시 체력 소모
- 음식 제작 시 즉시 회복 금지
- 음식 장착·즉시 섭취·체력 0 자동 섭취·마지막 수량 정리
- HUD 장비 5칸+음식 1칸
- 전 지역 강화돌 확률
- 장비 보관함·제작 잠금·허름한 장비 폐기 금지
- 귀환문·세계수·엔딩 세이브 삭제·이스터에그 메타

`tools/validate_project.py`는 이미지·오디오·런타임 경로·모바일 CSS·앱 버전·Windows 콘솔 억제·Android 몰입 패치와 짧은 앱 이름을 검사합니다.

## 실기기 최종 확인 항목

1. Android 첫 실행에서 상태·내비게이션 바가 숨겨지고 스와이프로 잠시 나타나는지
2. 런처 이름이 `이세계나무꾼`으로 잘리지 않고 같은 도끼 아이콘이 표시되는지
3. 연못 직접 탭 한 번으로 투척과 입질 대기가 시작되는지
4. 앱 강제 종료·백그라운드 후 휴식과 음식 포함 오토 정산이 정확한지
5. Windows 설치본 실행 시 콘솔 창이 나타나지 않는지
6. 360×640부터 태블릿까지 6칸 HUD와 마이페이지 음식 목록이 잘리지 않는지
